name: Publish to NPM

on:
  release:
    types: [published]

permissions:
  contents: read

jobs:
  validate-release:
    name: Validate Release
    runs-on: ubuntu-latest
    if: github.event.release.target_commitish == 'main'

    steps:
      - name: Check branch
        run: |
          echo "Release created from branch: ${{ github.event.release.target_commitish }}"
          if [ "${{ github.event.release.target_commitish }}" != "main" ]; then
            echo "Error: Releases can only be published from the main branch"
            exit 1
          fi

  test:
    name: Run Tests
    needs: validate-release
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [18.x, 20.x, 22.x]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.release.target_commitish }}

      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm test

  lint:
    name: Run Linting
    needs: validate-release
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.release.target_commitish }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20.x"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Run ESLint
        run: npm run lint

      - name: Check formatting
        run: npm run format:check

  publish:
    name: Publish to NPM
    needs: [test, lint]
    runs-on: ubuntu-latest

    # id-token is for provenance only. Publishing authenticates with the
    # NPM_TOKEN secret, which must be a granular access token with read and
    # write access to @src-dev/eleventy-template-asset-pipeline.
    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.release.target_commitish }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22.x"
          registry-url: "https://registry.npmjs.org"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      # Fail here, with a usable message, rather than with a bare 403 after the
      # tarball has been built and the provenance statement signed.
      - name: Check NPM credentials
        run: |
          if [ -z "$NODE_AUTH_TOKEN" ]; then
            echo "Error: the NPM_TOKEN secret is empty or missing."
            exit 1
          fi
          if ! NPM_USER=$(npm whoami 2>&1); then
            echo "Error: NPM_TOKEN did not authenticate with the registry."
            echo "npm said: $NPM_USER"
            echo "Generate a new granular access token on npmjs.com and update the NPM_TOKEN secret."
            exit 1
          fi
          echo "Authenticated as: $NPM_USER"
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Extract version from release tag
        id: get_version
        run: |
          VERSION="${{ github.event.release.tag_name }}"
          # Remove 'v' prefix if present
          VERSION="${VERSION#v}"
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Publishing version: $VERSION"

      - name: Verify package.json version matches release
        run: |
          PACKAGE_VERSION=$(node -p "require('./package.json').version")
          RELEASE_VERSION="${{ steps.get_version.outputs.version }}"
          echo "package.json version: $PACKAGE_VERSION"
          echo "Release tag version: $RELEASE_VERSION"
          if [ "$PACKAGE_VERSION" != "$RELEASE_VERSION" ]; then
            echo "Error: package.json version ($PACKAGE_VERSION) does not match release tag ($RELEASE_VERSION)"
            exit 1
          fi

      - name: Publish to NPM
        run: npm publish --provenance --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Publish summary
        run: |
          echo "### :rocket: Published to NPM" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Package:** @src-dev/eleventy-template-asset-pipeline" >> $GITHUB_STEP_SUMMARY
          echo "**Version:** ${{ steps.get_version.outputs.version }}" >> $GITHUB_STEP_SUMMARY
          echo "**Release:** ${{ github.event.release.html_url }}" >> $GITHUB_STEP_SUMMARY
          echo "**NPM:** https://www.npmjs.com/package/@src-dev/eleventy-template-asset-pipeline/v/${{ steps.get_version.outputs.version }}" >> $GITHUB_STEP_SUMMARY
